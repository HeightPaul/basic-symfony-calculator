{% extends 'base.html.twig' %}

{% block body %}
<div class="jumbotron">
    <div class="d-flex justify-content-center">
        <h1 class="display-4">Basic math calculations</h1>
    </div>
    <div class="d-flex justify-content-center">
        {{ form(form) }}
    </div>
</div>
{% endblock %}